The file curl2.rb acts as a server that responds to the requests it receives.
When you go to the route ‘/‘ it get the content of the file “hello.txt”
We get the content because we use the File.read method. 